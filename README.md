# note-app

To run the project in one terminal:
`npm install`
`npm start`

Open cypress in another terminal:
`npm run cy:open`

Go to http://localhost:8080

Test file:
.
    ├── cypress                       
    │   ├── integration             
    │   │    ├── notes.spec.js           
    └── ...